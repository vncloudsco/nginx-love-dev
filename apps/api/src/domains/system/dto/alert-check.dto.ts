import { ApiResponse } from '../../../shared/types/common.types';

/**
 * Response DTO for alert check trigger
 */
export interface AlertCheckTriggerResponseDto extends ApiResponse {
  message: string;
}
